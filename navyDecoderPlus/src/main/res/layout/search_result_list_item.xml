<?xml version="1.0" encoding="utf-8"?>

<!-- Layout for list items in the search results.

     Each row sets textAppearance for the M3 type role (weight/letter-spacing)
     and a separate textSize from dimens.xml so the row still follows the app's
     tablet-responsive scale instead of the type role's fixed size. -->

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  android:minHeight="48dp"
  android:background="?attr/selectableItemBackground"
  android:orientation="vertical"
  android:padding="@dimen/contentArea_padding">

  <TextView
    android:id="@+id/searchScreenResultItemToDecodeTextView"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:textAppearance="?attr/textAppearanceTitleSmall"
    android:textSize="@dimen/applicationTheme_textSize" />

  <TextView
    android:id="@+id/searchScreenResultDecodeInfoTextView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:maxLines="1"
    android:textAppearance="?attr/textAppearanceBodySmall"
    android:textSize="@dimen/resultDescription_textSize" />

  <TextView
    android:id="@+id/searchScreenResultCategoryTextView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:textAppearance="?attr/textAppearanceLabelSmall"
    android:textColor="?attr/colorOnSurfaceVariant"
    android:textSize="@dimen/resultDescription_textSize"
    android:visibility="gone" />

</LinearLayout>
