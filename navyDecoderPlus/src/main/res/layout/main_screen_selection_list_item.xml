<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  android:minHeight="48dp"
  android:background="?attr/selectableItemBackground"
  android:paddingStart="@dimen/listItem_paddingStart"
  android:paddingEnd="@dimen/listItem_paddingEnd"
  android:paddingTop="@dimen/listItem_paddingVertical"
  android:paddingBottom="@dimen/listItem_paddingVertical">

  <TextView
    android:id="@android:id/text1"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_centerVertical="true"
    android:layout_toStartOf="@+id/chevron"
    android:gravity="start"
    android:textColor="@color/resultDescription_TextColor"
    android:textSize="@dimen/mainScreenItemsToSelect_textSize" />

  <ImageView
    android:id="@+id/chevron"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_alignParentEnd="true"
    android:layout_centerVertical="true"
    android:contentDescription="@null"
    android:src="@drawable/ic_chevron_right"
    app:tint="@color/mainDecodeDescription_TextColor" />

</RelativeLayout>
